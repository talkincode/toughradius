{
  "schemas": [
    {
      "key": "radius.EapMethod",
      "type": "string",
      "default": "eap-md5",
      "enum": ["eap-md5", "eap-mschapv2", "eap-tls", "eap-peap", "eap-ttls"],
      "title": "EAP Method",
      "title_i18n": "config.radius.eap_method.title",
      "description": "EAP authentication method. eap-peap reuses the EAP-TLS server certificate/key (EapTlsCertFile/EapTlsKeyFile) to build the outer TLS tunnel and runs EAP-MSCHAPv2 inside it for Windows/AD compatibility; the inner MS-CHAPv2 carries an NTLMv1-like attack surface (per Microsoft), so keep the outer TLS strong and prefer eap-tls where clients support certificates. eap-ttls (RFC 5281) likewise builds a server-only TLS tunnel and authenticates legacy inner credentials for LDAP / legacy / mixed back ends: inner PAP (the cleartext password is protected only by the TLS tunnel) and inner MS-CHAP-V2 (which shares the same NTLMv1-like attack surface). Both eap-peap and eap-ttls require EapTlsCertFile/EapTlsKeyFile and pin the tunnel to TLS 1.2; choose eap-tls when every client can present a certificate.",
      "description_i18n": "config.radius.eap_method.description"
    },
    {
      "key": "radius.EapEnabledHandlers",
      "type": "string",
      "default": "*",
      "title": "Enabled EAP Handlers",
      "title_i18n": "config.radius.eap_enabled_handlers.title",
      "description": "Comma-separated list of enabled EAP handler names (e.g., eap-md5,eap-mschapv2). Use * to allow all registered handlers",
      "description_i18n": "config.radius.eap_enabled_handlers.description"
    },
    {
      "key": "radius.EapTlsCertFile",
      "type": "string",
      "default": "",
      "title": "EAP-TLS Server Certificate",
      "title_i18n": "config.radius.eap_tls_cert_file.title",
      "description": "Path to the PEM server certificate the RADIUS server presents during the EAP-TLS handshake (RFC 5216 §2.2). Leave empty to keep EAP-TLS disabled",
      "description_i18n": "config.radius.eap_tls_cert_file.description"
    },
    {
      "key": "radius.EapTlsKeyFile",
      "type": "string",
      "default": "",
      "title": "EAP-TLS Server Private Key",
      "title_i18n": "config.radius.eap_tls_key_file.title",
      "description": "Path to the PEM private key matching the EAP-TLS server certificate. Leave empty to keep EAP-TLS disabled",
      "description_i18n": "config.radius.eap_tls_key_file.description"
    },
    {
      "key": "radius.EapTlsCaFile",
      "type": "string",
      "default": "",
      "title": "EAP-TLS Client CA Bundle",
      "title_i18n": "config.radius.eap_tls_ca_file.title",
      "description": "Path to the PEM CA bundle used to verify EAP-TLS client certificate chains (RFC 5216 §2.2/§5.3). Leave empty to keep EAP-TLS disabled",
      "description_i18n": "config.radius.eap_tls_ca_file.description"
    },
    {
      "key": "radius.EapTlsMinVersion",
      "type": "string",
      "default": "1.2",
      "enum": ["1.2", "1.3"],
      "title": "EAP-TLS Minimum TLS Version",
      "title_i18n": "config.radius.eap_tls_min_version.title",
      "description": "Minimum TLS protocol version accepted for EAP-TLS handshakes",
      "description_i18n": "config.radius.eap_tls_min_version.description"
    },
    {
      "key": "radius.IgnorePassword",
      "type": "bool",
      "default": "false",
      "title": "Ignore Password",
      "title_i18n": "config.radius.ignore_password.title",
      "description": "Ignore password check",
      "description_i18n": "config.radius.ignore_password.description"
    },
    {
      "key": "radius.AccountingHistoryDays",
      "type": "int",
      "default": "90",
      "min": 0,
      "title": "Accounting History Days",
      "title_i18n": "config.radius.accounting_history_days.title",
      "description": "Accounting log retention days (0=disabled)",
      "description_i18n": "config.radius.accounting_history_days.description"
    },
    {
      "key": "radius.AcctInterimInterval",
      "type": "int",
      "default": "300",
      "min": 60,
      "max": 3600,
      "title": "Accounting Interim Interval",
      "title_i18n": "config.radius.acct_interim_interval.title",
      "description": "Accounting interim interval in seconds",
      "description_i18n": "config.radius.acct_interim_interval.description"
    },
    {
      "key": "radius.SessionTimeout",
      "type": "int",
      "default": "3600",
      "min": 300,
      "max": 86400,
      "title": "Session Timeout",
      "title_i18n": "config.radius.session_timeout.title",
      "description": "Default session timeout in seconds",
      "description_i18n": "config.radius.session_timeout.description"
    },
    {
      "key": "radius.LogLevel",
      "type": "string",
      "default": "info",
      "enum": ["debug", "info", "warn", "error"],
      "title": "Log Level",
      "title_i18n": "config.radius.log_level.title",
      "description": "RADIUS service log level",
      "description_i18n": "config.radius.log_level.description"
    },
    {
      "key": "radius.RejectDelayMaxRejects",
      "type": "int",
      "default": "7",
      "min": 1,
      "max": 1000,
      "title": "Reject Delay Threshold",
      "title_i18n": "config.radius.reject_delay_max_rejects.title",
      "description": "Maximum consecutive rejects before temporary block",
      "description_i18n": "config.radius.reject_delay_max_rejects.description"
    },
    {
      "key": "radius.RejectDelayWindowSeconds",
      "type": "int",
      "default": "10",
      "min": 1,
      "max": 3600,
      "title": "Reject Delay Window",
      "title_i18n": "config.radius.reject_delay_window_seconds.title",
      "description": "Observation window (seconds) for reject counter reset",
      "description_i18n": "config.radius.reject_delay_window_seconds.description"
    },
    {
      "key": "ldap.Enabled",
      "type": "bool",
      "default": "false",
      "title": "LDAP Backend Enabled",
      "title_i18n": "config.ldap.enabled.title",
      "description": "Enable the LDAP / Active Directory bind authentication backend. PAP-family only (bare PAP and EAP-TTLS inner PAP); challenge/response methods (CHAP/MS-CHAP/MS-CHAPv2/EAP-MD5/PEAP-MSCHAPv2) cannot be served because a bind needs the cleartext password. Default off.",
      "description_i18n": "config.ldap.enabled.description"
    },
    {
      "key": "ldap.ServerURL",
      "type": "string",
      "default": "",
      "title": "LDAP Server URL",
      "title_i18n": "config.ldap.server_url.title",
      "description": "Directory URL, e.g. ldap://dc.example.com:389 or ldaps://dc.example.com:636. Use ldaps:// for TLS, or ldap:// together with StartTLS.",
      "description_i18n": "config.ldap.server_url.description"
    },
    {
      "key": "ldap.BindMode",
      "type": "string",
      "default": "template",
      "enum": ["template", "search"],
      "title": "LDAP Bind Mode",
      "title_i18n": "config.ldap.bind_mode.title",
      "description": "template: build the user DN by substituting the username into BindDNTemplate. search: bind as a service account, locate the user DN with UserFilter under BaseDN, then re-bind as the user.",
      "description_i18n": "config.ldap.bind_mode.description"
    },
    {
      "key": "ldap.BindDNTemplate",
      "type": "string",
      "default": "",
      "title": "LDAP Bind DN Template",
      "title_i18n": "config.ldap.bind_dn_template.title",
      "description": "Template mode only. DN template with a single %s for the username, e.g. uid=%s,ou=people,dc=example,dc=com or the Active Directory UPN form %s@example.com.",
      "description_i18n": "config.ldap.bind_dn_template.description"
    },
    {
      "key": "ldap.BaseDN",
      "type": "string",
      "default": "",
      "title": "LDAP Search Base DN",
      "title_i18n": "config.ldap.base_dn.title",
      "description": "Search mode only. Subtree base for the user lookup, e.g. dc=example,dc=com.",
      "description_i18n": "config.ldap.base_dn.description"
    },
    {
      "key": "ldap.UserFilter",
      "type": "string",
      "default": "(uid=%s)",
      "title": "LDAP User Filter",
      "title_i18n": "config.ldap.user_filter.title",
      "description": "Search mode only. LDAP filter with a single %s for the username (escaped before substitution), e.g. (uid=%s) or Active Directory (sAMAccountName=%s).",
      "description_i18n": "config.ldap.user_filter.description"
    },
    {
      "key": "ldap.SearchBindDN",
      "type": "string",
      "default": "",
      "title": "LDAP Service Account DN",
      "title_i18n": "config.ldap.search_bind_dn.title",
      "description": "Search mode only. DN of the read-only service account used to search for users, e.g. cn=svc-radius,ou=svc,dc=example,dc=com.",
      "description_i18n": "config.ldap.search_bind_dn.description"
    },
    {
      "key": "ldap.SearchBindPassword",
      "type": "string",
      "default": "",
      "title": "LDAP Service Account Password",
      "title_i18n": "config.ldap.search_bind_password.title",
      "description": "Search mode only. Password for the service account DN.",
      "description_i18n": "config.ldap.search_bind_password.description"
    },
    {
      "key": "ldap.StartTLS",
      "type": "bool",
      "default": "false",
      "title": "LDAP StartTLS",
      "title_i18n": "config.ldap.start_tls.title",
      "description": "Upgrade an ldap:// connection to TLS with StartTLS before binding (RFC 4513 §3). Leave off when using an ldaps:// URL.",
      "description_i18n": "config.ldap.start_tls.description"
    },
    {
      "key": "ldap.TLSSkipVerify",
      "type": "bool",
      "default": "false",
      "title": "LDAP Skip TLS Verify",
      "title_i18n": "config.ldap.tls_skip_verify.title",
      "description": "Skip TLS certificate verification. Insecure; use only for lab or self-signed directories.",
      "description_i18n": "config.ldap.tls_skip_verify.description"
    },
    {
      "key": "ldap.Timeout",
      "type": "int",
      "default": "5",
      "min": 1,
      "max": 60,
      "title": "LDAP Timeout",
      "title_i18n": "config.ldap.timeout.title",
      "description": "Dial and per-operation timeout in seconds.",
      "description_i18n": "config.ldap.timeout.description"
    }
  ]
}